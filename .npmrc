# Suppress ELIFECYCLE errors that appear on Ctrl+C
# This makes npm exit cleanly without showing "Command failed" messages
loglevel=error
progress=false
# Disable lifecycle script error reporting
quiet=true
# Don't bail on optional dependencies
optional=false
