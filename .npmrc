# npm-specific configuration (clean config without pnpm settings)
save-exact=true

# Explicitly unset potentially problematic global npm configs that cause warnings
verify-deps-before-run=false
_jsr-registry=

# Security settings
fund=false
audit=false
