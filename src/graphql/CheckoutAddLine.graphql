mutation CheckoutAddLine($id: ID!, $lines: [CheckoutLineInput!]!) {
	checkoutLinesAdd(id: $id, lines: $lines) {
		checkout {
			...CheckoutFragment
		}
		errors {
			field
			code
			message
		}
	}
}
